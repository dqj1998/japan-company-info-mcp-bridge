# Dependencies
node_modules/

# OS / editor cruft
.DS_Store
*.swp

# Logs & env
*.log
.env
.env.*

# NOTE: bin/ (runtime binaries) and assets/ (Orb bundle) are INTENTIONALLY
# committed — npx-from-GitHub relies on bin/ (npm-published tarball no longer does;
# it uses per-platform optionalDependencies instead — see index.js resolveRuntime).

# Generated per-platform runtime packages (materialized from bin/ at publish time)
runtime-packages/
